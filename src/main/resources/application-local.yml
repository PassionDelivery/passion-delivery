server:
  port: 8080

spring:
  application:
    name: pdelivery
  datasource:
    username:
    password:
    url:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 100
